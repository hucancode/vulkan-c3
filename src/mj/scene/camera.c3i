module mj;

enum CameraProjection {
    PERSPECTIVE,
    ORTHOGRAPHIC
}

struct Camera {
    CameraProjection projection;
    float fov;
    float aspect_ratio;
    float near;
    float far;
    float[<4>] up;
}
